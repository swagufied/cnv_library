from setuptools import setup

setup(name='cnv_lib',
      version='dev',
      description='library to quickly analyze cnv calls',
      url='https://github.com/swagufied/cnv_library',
      author='swagufied',
      license='MIT',
      packages=['cnv_lib'],
      zip_safe=False)